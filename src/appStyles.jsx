import teal from '@material-ui/core/colors/teal';

const appStyles = {
   app : {
       backgroundColor: 'black',
       color: teal['A200'],
       height: '100vh',
   }
};

export default appStyles;
